<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.farmacumplido">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FarmaCumplido">
        <activity android:name=".MostrarDetalleMedicamentoctivity"/>
        <activity android:name=".NuevoMedicamentosActivity" />
        <activity android:name=".MostrarMedicamentosActivity1" />
        <activity android:name=".ActualizarProveedorActivity2" />
        <activity android:name=".ActualizarProveedorActivity1" />
        <activity android:name=".BorrarProveedorActivity" />
        <activity android:name=".NuevoProveedorActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>